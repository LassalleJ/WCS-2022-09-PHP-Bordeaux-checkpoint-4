{% extends 'base.html.twig' %}

{% block title %}New Group{% endblock %}

{% block body %}
    {% include '_navbar-connected.html.twig' %}
    <h1 class="section-title">Create your Group</h1>

    {{ form_start(form) }}
    <div class="mx-auto col-8">
    {{ form_widget(form) }}
    <button class="btn btn-primary col-12 mt-5">{{ button_label|default('Save') }}</button>
    </div>
    {{ form_end(form) }}
{% endblock %}
